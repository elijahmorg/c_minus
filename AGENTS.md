C-minus is a C language variant that transpiles to C

See ./README.md and ./c_minus.md for details.

All code should be written in good Go style and following good practices
This code should have Go unit tests covering our use cases

Any docs you generate that I don't explicitly tell you otherwise should go in ai_docs they should be lower case snake case.

Never ever add timeline estimates.
Never use emojis

When working on something always run the unit tests and then the integration tests.

When we add new features add new UTs and integration tests - e.g. sample2
